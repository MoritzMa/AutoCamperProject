import javafx.scene.control.Label;
import javafx.scene.control.ScrollPane;
import javafx.scene.layout.VBox;

public class ShowAutocampers {
    /*public ScrollPane showthattype(Object AutoCamperType)
    {
        ScrollPane ACScrollPane = new ScrollPane();
        VBox ACVbox = new VBox();
        Label test = new Label("tester");
        ACVbox.getChildren().add(test);
        ACScrollPane.setContent(ACVbox);
        return ACScrollPane;
    }
}*/}
